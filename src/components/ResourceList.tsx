import styled from "styled-components";

const Container = styled.div`
  background-color: yellow;
  width: 280px;
  border-right: 1px solid #c4c4c4;
`;

const ResourceList = () => {
  return <Container>ResourceList</Container>;
};

export default ResourceList;
